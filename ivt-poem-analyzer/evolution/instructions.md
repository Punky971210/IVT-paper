# Instructions

> Auto-generated from evolutions.json. Do not edit directly.

<a id="ev_8b63364f"></a>
### [ev_8b63364f] 在IVT诗作分析的隐式/显式构建切换分析（5a）中，引入隐式主体功能标记S_隐(观)=隐式感知者和S_隐(行)=隐式行动者，当读者运算端出现与作者构建的隐式'我'行为不同步时（如先看风景...
## 5a. 隐式/显式构建切换分析（精化版）

### 隐式主体功能标记

在分析隐式构建（诗中无显式"我"但读者感知到隐含主体）时，需进一步区分隐式主体的功能类型：

- **S_隐(观) = 隐式感知者**：隐式"我"作为纯粹感知/观看的主体，被动接收外界意象输入。如"你站在桥上看风景"——读者通过"你"的视角观看风景，隐式主体是感知承载者。
- **S_隐(行) = 隐式行动者**：隐式"我"作为主动行动/行为的主体，对外界施加动作。如"我打江南走过"——隐式主体是行动发起者。

### 功能切换判定

当读者的运算端出现与作者构建的隐式"我"行为不同步时，视为一次**隐式构建的功能切换**：

- **同一隐式主体内部的功能切换**：隐式"我"从纯感知（观）转变为行动（行），或反之。例如：读者先在静态感知场景（"看风景"）中定位隐式主体，随后该主体做出行动（"装饰"），此时读者需重新校准对隐式主体的认知——从感知者切换为行动者。
- **切换计次规则**：每次隐式主体的功能类型发生变化，在 $N_{switch}$ 中计为1次。切换路径表达为：$S_{隐(观)} \rightarrow S_{隐(行)}$ 或 $S_{隐(行)} \rightarrow S_{隐(观)}$。

### 分析示例

以卞之琳《断章》为例：
- "你站在桥上看风景" → 隐式主体定位为 S_隐(观)，"你"是观看行为承载者
- "明月装饰了你的窗子" → 同一隐式主体"明月"从被观看的标的切换为主动施动者（装饰行为），S_隐(观) → S_隐(行) 切换
- **功能切换计次**：1次

### 分析输出格式

在原有隐式/显式切换分析基础上增加功能标记列：

| 隐式主体 | 功能类型 | 切换路径 | 切换计次 |
|---------|---------|---------|---------|
| "你"（桥上看风景段） | S_隐(观) | — | — |
| "明月"（装饰段） | S_隐(行) | S_隐(观)→S_隐(行) | 1 |

*Source: user_intent | 2026-07-12T14:03:00.132929+00:00*

---
